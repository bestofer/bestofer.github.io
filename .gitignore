# build output (generated by `node build.mjs`; rebuilt in CI on deploy)
dist/

# private ops docs — map account emails, must NOT be published (repo is public)
SITE-OPERATIONS.txt
SITE-OPERATIONS.html

# deps / os / editor cruft
node_modules/
.DS_Store
Thumbs.db
*.log
~$*
